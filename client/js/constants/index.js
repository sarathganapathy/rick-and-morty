/**
 * @constant
 * @enum { Object } enum for default values.
 */
export const DEFAULT_VALUE = {
  NO_RESULTS: "--"
};

/**
 * @constant
 * @enum { Object } enum for sort orders.
 */
export const SORT_ORDER = {
  ASCENDING: "ascending",
  DESCENDING: "descending"
};

/**
 * @constant
 * @enum { Object } enum for sort key.
 */
export const SORT_KEY = {
  ID: "id",
  NAME: "name"
};
